  # Connect on a TCP socket. Omitted by default since the client uses a
common: &common
    adapter: postgresql
    username: postgres
    password: password
    host: localhost
    port: 5432
    
development:
    <<: *common
    database: destroy_multiple_development

test:
    <<: *common
    database: destroy_multiple_test

production:
    <<: *common
    database: destroy_multiple_production
  
