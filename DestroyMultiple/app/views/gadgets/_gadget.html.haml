%tr{ :id => 'gadget_#{gadget.id}'}
  %td= gadget.created_at.localtime.strftime("%B %d, %l:%M%p") 
  %td= gadget.name
  %td.delete{:style => 'text-align:center;'}
    =check_box_tag "gadget_ids[]", gadget.id, false, :class => "checkbox", :id => "gadget_#{gadget.id}"